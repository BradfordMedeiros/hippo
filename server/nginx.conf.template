
events {
    worker_connections   1024;
}

http {
	server {
  	listen        {{PORT}};

 	 	location {{BASE_ROUTE}} {
    			autoindex on;
    			root  {{CONTENT_FOLDER}};

  		} 
	   
	}	
}

daemon off;
